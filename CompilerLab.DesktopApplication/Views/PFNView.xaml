<UserControl d:DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=PFNViewModel}"  x:Class="CompilerLab.DesktopApplication.Views.PFNView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CompilerLab.DesktopApplication.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>

            <RowDefinition Height="20"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBox Name="TxtBoxInput" Text="{Binding InputString}"  Grid.Row="0"></TextBox>
        <Button Name="BtnEvaluate" Command="{Binding EvaluateCommand}"  Grid.Row="1">Evaluate</Button>
        <TextBlock Name="TxtBlkOutput" Text="{Binding IntermediatePFNString}"  Grid.Row="2"></TextBlock>
        <TextBlock Name="TxtBlkError" Text="{Binding ErrorString}"  Foreground="Red"  Grid.Row="3"></TextBlock>
    </Grid>
</UserControl>
